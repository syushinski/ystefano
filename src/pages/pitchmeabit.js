import React from "react"

import Layout from "../components/layout"
import SEO from "../components/seo"

const PitchMeABitPage = () => (
  <Layout>
    <SEO title="Pitch me a bit" />
    <div style={{maxWidth: 600, width: 600}}>
      <h1>Pitch me a bit</h1>
      <p>Here's where you'll be able to write a one or two liner bit pitch. I have no where for you to get it to me at the moment, but think about some "guy whos" or "what ifs". </p>
    </div>
  </Layout>
)

export default PitchMeABitPage